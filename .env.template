# Fichier template pour les variables d'environnement
# Copiez ce fichier en .env et modifiez les valeurs selon vos besoins

# Environnement (development/production)
NODE_ENV=development

# Configuration Base de données
DB_NAME=framadate
DB_USER=framauser
DB_PASSWORD=changeme_secure_password

# JWT Secret (utilisez une clé forte en production)
JWT_SECRET=your-super-secret-jwt-key-change-me

# URLs (pour le développement local)
API_URL=http://localhost:4001

# Dockerfile à utiliser (Dockerfile ou Dockerfile.dev)
DOCKER_FILE=Dockerfile.dev